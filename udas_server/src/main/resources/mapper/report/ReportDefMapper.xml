<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boshz.udas.report.mapper.ReportDefMapper">

    <select id="getList" resultType="com.boshz.udas.report.entity.ReportDef">
        SELECT * FROM tb_report_def
        WHERE del_flag = 0
        <!-- 普通用户只能看自己创建的数据 -->
        <if test="account != null">
            AND create_by = #{account}
        </if>
        <if test="query.reportName != null and query.reportName != ''">
            AND report_name LIKE CONCAT('%', #{reportName}, '%')
        </if>
        ORDER BY create_time DESC
        LIMIT #{begin}, #{pageSize}
    </select>
    <select id="getListTotal" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM tb_report_def
        WHERE del_flag = 0
        <!-- 普通用户只能看自己创建的数据 -->
        <if test="account != null">
            AND create_by = #{account}
        </if>
        <if test="query.reportName != null and query.reportName != ''">
            AND report_name LIKE CONCAT('%', #{reportName}, '%')
        </if>
    </select>

    <select id="selectAll" resultType="com.boshz.udas.report.entity.ReportDef">
        select id, report_code, report_name, table_name
        from tb_report_def
    </select>

    <select id="selectByCode" parameterType="java.lang.String" resultType="com.boshz.udas.report.entity.ReportDef">
        select id, report_code, report_name, table_name
        from tb_report_def
        where report_code = #{reportCode}
    </select>

    <select id="countByCodePrefix" resultType="int">
        select count(*) from tb_report_def where report_code like concat(#{prefix},'%')
    </select>
    <select id="selectReportList" resultType="com.boshz.udas.report.entity.ReportDef">
        select id, report_code, report_name, table_name
        from tb_report_def
        where report_code = #{reportCode}
    </select>
    <select id="findByPath" resultType="com.boshz.udas.report.entity.Menu">
        select id, app_id, parent_id, name, path, icon, hide_in_menu, copy, sort, line_id, access_line_id, description
        from tb_menu
        where path = #{path}
    </select>

    <select id="getListByPage" resultType="com.boshz.udas.vo.PageVo">
        select id, report_code, report_name, table_name
        from tb_report_def
    </select>

    <update id="updateStatus">
        UPDATE tb_report_def
        SET status = #{status}
        WHERE id = #{id}
    </update>


    <!-- 插入 -->
    <insert id="insert" parameterType="com.boshz.udas.report.entity.ReportDef">
        insert into tb_report_def (
            report_code,
            report_name,
            table_name
        ) values (
                     #{reportCode},
                     #{reportName},
                     #{tableName}
                 )
    </insert>
</mapper>