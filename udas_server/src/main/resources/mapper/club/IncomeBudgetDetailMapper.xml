<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boshz.udas.club.mapper.IncomeBudgetDetailMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_income_budget_detail(item_id,subject_code,budget_amount,actual_amount,budget_year,remark,create_by)
        VALUES(#{itemId},#{subjectCode},#{budgetAmount},#{actualAmount},#{budgetYear},#{remark},#{createBy})
    </insert>
    <update id="update">
        UPDATE tb_income_budget_detail
        <set>
            <if test="itemId != null">item_id=#{itemId},</if>
            <if test="subjectCode != null">subject_code=#{subjectCode},</if>
            <if test="budgetAmount != null">budget_amount=#{budgetAmount},</if>
            <if test="actualAmount != null">actual_amount=#{actualAmount},</if>
            <if test="budgetYear != null">budget_year=#{budgetYear},</if>
            <if test="remark != null">remark=#{remark},</if>
            update_time = NOW()
        </set>
        WHERE id=#{id}
    </update>
    <delete id="deleteById">DELETE FROM tb_income_budget_detail WHERE id=#{id}</delete>
    <select id="selectById" resultType="com.boshz.udas.club.entity.IncomeBudgetDetail">
        SELECT id,item_id itemId,subject_code subjectCode,budget_amount budgetAmount,actual_amount actualAmount,budget_year budgetYear,remark,create_by createBy,create_time createTime,update_time updateTime
        FROM tb_income_budget_detail WHERE id=#{id}
    </select>
    <select id="selectPage" resultType="com.boshz.udas.club.entity.IncomeBudgetDetail">
        SELECT id,item_id itemId,subject_code subjectCode,budget_amount budgetAmount,actual_amount actualAmount,budget_year budgetYear,remark,create_by createBy,create_time createTime,update_time updateTime
        FROM tb_income_budget_detail
        <where>
            <if test="query.subjectCode != null and query.subjectCode != ''">subject_code=#{query.subjectCode}</if>
            <if test="query.itemId != null">item_id=#{query.itemId}</if>
            <if test="query.budgetYear != null">budget_year=#{query.budgetYear}</if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset},#{pageSize}
    </select>
    <select id="selectCount" resultType="Long">
        SELECT COUNT(1) FROM tb_income_budget_detail
        <where>
            <if test="query.subjectCode != null and query.subjectCode != ''">subject_code=#{query.subjectCode}</if>
            <if test="query.itemId != null">item_id=#{query.itemId}</if>
            <if test="query.budgetYear != null">budget_year=#{query.budgetYear}</if>
        </where>
    </select>
</mapper>